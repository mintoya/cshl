const sliceStruct = @anon{
  ptr *u8,
  len usize;
};

fn slice (t type) type {
  return @anon{
    ptr *t,
    len usize;
    fn [](self @this,idx usize) *t {
      return @cast(self.ptr , [self.len]type)[idx];
    }
  };
}
